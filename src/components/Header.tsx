import logo from "../assets/images/logo.svg";

const Header = () => {
  return (
    <header className="">
      <img src={logo} alt="URBN User Lookup" />
    </header>
  );
};

export default Header;
